# ShipSafe's own source code — a security tool legitimately reads files,
# makes HTTP calls, parses configs, etc. These are not vulnerabilities.
src/
tests/

# kuzu is deprecated but deeply integrated in the graph engine; suppress until migration
package.json

# Packages have separate scan cycles
packages/

# CLI entry point
bin/
